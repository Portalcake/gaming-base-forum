= semantic_form_for([@topic, @topic.posts.build]) do |f|
  = f.inputs do 
    = f.input :content, :label=>false
  = f.actions do 
    = f.action :submit, :button_html => { :disable_with => 'Working...' }